{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if(!ua.includes('pnpm')){console.error('Use pnpm instead'); process.exit(1)}\"",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "api:dev": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/ul-ai-assistant run dev",
    "web:dev": "pnpm --filter @workspace/ul-ai-assistant run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "sync": "node -e \"fetch('http://127.0.0.1:8080/api/sync/now',{method:'POST'}).then(r=>r.json()).then(console.log).catch(e=>{console.error(e);process.exit(1)})\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
